export type ReportStatus = "new" | "in_progress" | "closed";
export type ReportSeverity = "low" | "medium" | "high" | "critical";
export type ReportEntityType = "post" | "campaign" | "user" | "organization";
export type ReportTimelineEntry = { id?: string; action?: string; status?: string; label?: string; by?: string; actor?: string; at?: string; timestamp?: string; note?: string };
export type NameObject = { id?: string; name: string; email?: string | null };
export type ReportParty = NameObject & { type?: "user" | "organization" };
export type ReportEntity = { type: ReportEntityType; id: string; data: Record<string, unknown> };
export type ReportItem = { id: string; title: string; description: string; status: ReportStatus; severity: ReportSeverity; entityType: ReportEntityType; entityId: string | null; entity: ReportEntity | null; reporter: ReportParty | null; reportedTarget: ReportParty | null; organizationName: string | NameObject | null; reporterName: string | NameObject | null; createdAt: string; assignee?: string | NameObject | null; timeline: ReportTimelineEntry[] | null; evidence: unknown; closedAt?: string | null };
export const reportStatusLabels: Record<ReportStatus, string> = { new: "جديد", in_progress: "قيد المعالجة", closed: "مغلق" };
export const reportSeverityLabels: Record<ReportSeverity, string> = { low: "منخفض", medium: "متوسط", high: "مرتفع", critical: "حرج" };
export const reportEntityTypeLabels: Record<ReportEntityType, string> = { post: "منشور", campaign: "حملة", user: "مستخدم", organization: "منظمة" };
