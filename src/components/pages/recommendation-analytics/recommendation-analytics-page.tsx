'use client'
import * as React from 'react'
import { useRecommendationAnalytics } from '@/features/admin/recommendations/admin.recommendations.query'

const labels: Record<string,string>={impressions:'مرات الظهور',openRate:'معدل فتح المحتوى',saveRate:'معدل الحفظ',helpOffers:'عروض المساعدة',applications:'طلبات التطوع',donations:'التبرعات',fulfilledRequests:'الطلبات المكتملة',recommendationToHelpRate:'التحويل من توصية إلى مساعدة'}
const percentKeys=new Set(['openRate','saveRate','recommendationToHelpRate'])
export function RecommendationAnalyticsPage(){const [days,setDays]=React.useState(30);const to=new Date();const from=new Date();from.setDate(to.getDate()-days+1);const q=useRecommendationAnalytics({from:from.toISOString().slice(0,10),to:to.toISOString().slice(0,10)});const k=q.data?.data.kpis;return <section className="space-y-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-semibold">تحليلات التوصيات</h2><p className="text-sm text-muted-foreground">قياس أثر التوصيات من الظهور حتى تقديم المساعدة.</p></div><select className="h-9 rounded-md border bg-background px-2 text-sm" value={days} onChange={e=>setDays(Number(e.target.value))}><option value={7}>7 أيام</option><option value={30}>30 يوم</option><option value={90}>90 يوم</option></select></div>{q.isError?<div className="rounded-md border border-destructive/30 p-3 text-sm text-destructive">تعذر تحميل تحليلات التوصيات.</div>:null}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{q.isLoading?Array.from({length:8}).map((_,i)=><div key={i} className="h-24 animate-pulse rounded-xl border bg-muted/50"/>):k?Object.entries(k).map(([key,value])=><div key={key} className="rounded-xl border bg-card p-4"><p className="text-xs text-muted-foreground">{labels[key]??key}</p><p className="mt-2 text-2xl font-bold">{value}{percentKeys.has(key)?'%':''}</p></div>):null}</div><div className="rounded-xl border bg-card p-4"><h3 className="text-sm font-semibold">Impression → Open → Help</h3><div className="mt-4 space-y-3">{q.data?.data.series.map(row=><div key={row.day} className="grid grid-cols-4 items-center gap-3 text-xs"><span>{row.day}</span><span>ظهور {row.impressions}</span><span>فتح {row.opens}</span><span>مساعدة {row.help_actions}</span></div>)}{!q.isLoading&&!q.data?.data.series.length?<p className="py-8 text-center text-sm text-muted-foreground">لا توجد بيانات توصيات ضمن الفترة المحددة.</p>:null}</div></div></section>}
